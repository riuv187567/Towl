<Window x:Class="Towl.WPF.TowlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Towl.WPF"
        mc:Ignorable="d"
        Title="Towl" WindowStyle="None" MouseLeftButtonDown="TowlWindowMouseLeftButtonDown" Topmost="True" ResizeMode="CanMinimize" Width="256" Height="160" Background="{x:Null}" AllowsTransparency="True" Foreground="{x:Null}">
    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarMinimizeButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#353639"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TitleBarCloseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F25022"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border CornerRadius="10" Background="#FF1F2023" BorderBrush="Gray" BorderThickness="0.5">
        <Grid Margin="0,4,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ButtonMinimize" Click="TowlWindowMinimize" Content="-"
                    Style="{StaticResource TitleBarMinimizeButtonStyle}"/>
                <Separator Opacity="0" Width="8"/>
                <Button x:Name="ButtonClose" Click="TowlWindowClose" Content="✕"
                    Style="{StaticResource TitleBarCloseButtonStyle}" Margin="0,0,8,0"/>
            </StackPanel>

            <WrapPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
                <TextBlock Name="MainTimeText" Text="00:00:00" Margin="0,0,0,0" FontFamily="Arial" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                <Rectangle Name="StatusBar" Height="4" HorizontalAlignment="Center" VerticalAlignment="Center" Width="96" RadiusY="4" RadiusX="4" Fill="#FFFF453A" />
                <TextBlock Name="SecondaryTimeText" Text="00:00:00" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#C4C5C6"/>
            </WrapPanel>
        </Grid>
    </Border>
</Window>
